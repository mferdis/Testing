const num = 321;
const maxRedigit = function(num){
    if(num < 100 || num > 999){
        return null
        return + num.toString().split().short((a,b) => b - a).join();
    }
    console.log(maxRedigit(num));
}